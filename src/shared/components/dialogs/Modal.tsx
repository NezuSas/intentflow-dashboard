import { Modal as AntModal, Typography } from "antd";import type { ReactNode } from "react";
export function Modal({open,title,description,onClose,children,footer}:{open:boolean;title:string;description?:string;onClose:()=>void;children:ReactNode;footer?:ReactNode}){return <AntModal open={open} title={title} onCancel={onClose} centered destroyOnHidden footer={footer ?? null}>{description&&<Typography.Paragraph type="secondary">{description}</Typography.Paragraph>}{children}</AntModal>}
